# Base de données de production - via variables d'environnement
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# JWT - via variable d'environnement
jwt.secret=${JWT_SECRET}

# Logging minimal
logging.level.com.edueasy.user=WARN